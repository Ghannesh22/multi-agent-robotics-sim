[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "marlsim"
version = "0.1.0"
description = "A simple multi-agent robotics grid-world simulation for learning coordination and reinforcement learning."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
